import {
    Stack
} from '@mui/material';
import { GridToolbar } from '@mui/x-data-grid';

export default function Header () {
    return (
        <Stack>
          {GridToolbar}
        </Stack>
    );
}